using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.Advertisements;

public class anunciosPrueba : MonoBehaviour {

    public NotaFinal x;

	// Use this for initialization
	void Start () {
        x.enabled = false;
        //Advertisement.Show();
	}
	
	// Update is called once per frame
	void Update () {/*
		if (Advertisement.isShowing == true)
        {
            x.enabled = false;
        }
       else if (Advertisement.isShowing == false)
       {
            x.enabled = true;
       }*/
	}
}
